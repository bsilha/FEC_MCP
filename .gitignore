__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.env

# PDF rulebooks ARE tracked in this repo (see data/rulebooks/). Only the
# derived search index cache is ignored, since it's rebuilt automatically.
data/rulebooks/.index/
.mcp.json
.claude/

# demo/app.py's _sync_static_pdfs mirrors data/rulebooks/ here at runtime
# so Streamlit's static file server can serve PDF-page-linking URLs --
# generated, not source data.
/demo/static/
